# Side Scroller

## Run

From this directory:

```bash
npm run dev
```

Then open `http://localhost:8000`.

## Notes

This project is a plain browser game loaded from `index.html` with ES modules in `js/`, so it should be served over HTTP instead of opened directly as a file.
